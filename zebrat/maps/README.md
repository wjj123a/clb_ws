Generated maps saved by `save_map_snapshot.py` land here as `<world_profile>.pgm` and `<world_profile>.yaml`.
The default runtime keeps `save_map:=false`; enable it explicitly when you want to persist a SLAM snapshot.
